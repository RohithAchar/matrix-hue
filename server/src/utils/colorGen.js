/*
 * colorGen.js — Server-side HSL color generation
 *
 * Same constraints as client/src/utils/colorGen.js
 *
 * Used by Global mode (server generates daily targets)
 *
 * TODO:
 * - generateTargets(difficulty, count) → array of {h,s,l}
 * - Same constraints per difficulty as client version
 * - Export for server usage (CommonJS)
 */

// TODO: implement color generation
