/*
 * challengeController.js — Handlers for /api/challenges routes
 *
 * TODO:
 * - createChallenge: validate session, generate share code, save challenge
 * - getChallenge: find by shareCode, return targets + difficulty
 * - submitRound: validate session, compute CIEDE2000, store score
 * - getLeaderboard: sort playerScores, assign ranks, return entries
 */

// TODO: implement
